<!-- Shared base: Claude environment / Codex AGENTS.md. Project notes: CLAUDE.local.md. -->
- Identify what the user is asking to learn, assess, or change from the conversation. Answer requests for information with relevant evidence; a question about industry practice needs external sources, not an unsolicited local policy proposal. Do not substitute rule-making or implementation for the question asked.
- Start from the user's purpose; keep uncertain interpretations provisional. For ambiguous or consequential work, explore plausible meanings, causes, users, and constraints. Gather evidence that distinguishes them, including contrary evidence; report sources, findings, and gaps. Define the problem and success criteria when remaining uncertainty would not change the next safe action, then compare viable solutions. Reopen the framing if evidence contradicts it. Scale exploration to ambiguity and impact; clear small requests proceed directly. Questions do not authorize changes. Proceed on authorized work; ask only when missing information materially changes the decision. WBS, Gantt, and effort reports are optional. Existing safety and verification gates still apply.
- Resume: `bash ~/.harness/bin/harness-journal --latest` in this worktree. Read only its latest summary and recheck relevant source/status/evidence.
- If a terse or spoken request admits materially different actions, state the relevant current behavior and your reading before editing; clarify before dependent work. Your reading, silence, or elapsed time is not confirmation or authorization. Keep unresolved readings in conversation/journal before a task exists, then in its premises when relevant; do not reconfirm clear requests.
- On startup/resume, present the housekeeping hook's read-only summary and ask whether to inspect now or skip. Before any cleanup, confirm the specific items; receipt of upstream reports is not resolution. Continue independent authorized work while awaiting a choice.
- Design and stack conventions: docs/DESIGN.md; update it when structure or stack changes. Use manifest/lockfile versions, existing tools, and primary documentation when needed.
- Commands: `make help`. Run applicable Makefile checks; done requires `make verify` with evidence tied to the deliverable. Explain and obtain approval before adopting a new dependency.
- Apply YAGNI and KISS. Read references only as needed; learning libraries and role memories are optional.
- Meet the required quality while reducing cost and latency. Delegation is relative to the main model (D178): judgement personas inherit it; worker subagents run one tier lighter (fable→opus→sonnet→haiku), named per call, when handoff, review, and retry costs still make sense.
- Handoff before ending: save goal, work done, checks/scope, unresolved work, and next action with `bash ~/.harness/bin/harness-journal --save <summary.md>` (8 KiB max; no credentials or raw transcripts). Use ignored scratch for the draft. Details: docs/METHOD.md §3 when present.
- Update affected README, requirements, design, and usage docs with behavior changes; before ending, check this task's documentation impact and verify necessary updates. Do not rewrite unrelated docs.
- Record material decisions in .backlog/DECISIONS.md (append-only). Keep docs/PLAN.md a short roadmap; issue state belongs in the chosen external PM tool, used by the rules in docs/ISSUES.md.
