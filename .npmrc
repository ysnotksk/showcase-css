# Project npm config — overrides ~/.npmrc (which sets ignore-scripts=true, min-release-age=7).
#
# If this project's dependencies require postinstall hooks (esbuild, electron,
# puppeteer, better-sqlite3, sharp, or other native modules), uncomment below
# and document which packages require it.
#
# ignore-scripts=false
